# Agent Manifest Schema

This directory holds the canonical schema and examples for the AgentFabric agent manifest (e.g. YAML/JSON Schema).

See [README.md](../../README.md) for an example manifest and the [Agent Protocol Layer](../../docs/agent-protocol-layer.md) for how manifests integrate with the runtime.
