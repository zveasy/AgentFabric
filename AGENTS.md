# AgentFabric

## Cursor Cloud specific instructions

This is currently an empty/greenfield repository with no application code. The only file is `README.md`.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No services** to run.
- **No lint, test, or build commands** are configured yet.

When application code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run the dev server, tests, and linter.
- Any non-obvious startup caveats or environment requirements.
